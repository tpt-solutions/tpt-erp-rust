[package]
name = "flow"
description = "Cross-vertical reference flow on TPT ERP: an OMS order triggers a WMS pick, which triggers a TMS dispatch route, which triggers a GL posting — orchestrated entirely over tpt-erp-bus."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
tpt-erp-primitives = { workspace = true }
tpt-erp-ledger = { workspace = true }
tpt-erp-bus = { workspace = true }
tpt-erp-tenant = { workspace = true }
gl = { path = "../gl" }
wms = { path = "../wms" }
tms = { path = "../tms" }
oms = { path = "../oms" }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
rust_decimal = { workspace = true }
chrono = { workspace = true }
futures = "0.3"
anyhow = "1"

[dev-dependencies]
uuid = { workspace = true }

[lints]
workspace = true
